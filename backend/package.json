{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "nodemon server.js",
    "start": "node server.js",
    "start:pm2": "pm2 start server.js --name backend --update-env",
    "stop:pm2": "pm2 delete backend || true",
    "test": "cross-env NODE_ENV=test node --experimental-vm-modules node_modules/jest/bin/jest.js --runInBand --detectOpenHandles"
  },
  "dependencies": {
    "express": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "pg": "^8.13.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
